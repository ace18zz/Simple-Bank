const StatementPrinter = require('../bank-challenge/src/StatementPrinter');

describe('StatementPrinter', () => {
    it('should construct without error', () => {
        expect(() => new StatementPrinter()).not.toThrow();
    });
//The main function of this function is to print the message to the console
});
